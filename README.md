# Print Layout Manager

**UXP плагин для Adobe Photoshop** - управление раскладкой принтов с интеграцией XLSX таблиц.

## 🎯 Возможности

- ✅ **Чтение XLSX таблиц** - загрузка данных о принтах из Excel-файлов
- ✅ **Синхронизация слоёв** - автоматическое сопоставление слоёв Photoshop с записями таблицы
- ✅ **Двусторонняя синхронизация** - выбор в плагине выделяет слой в Photoshop и наоборот
- ✅ **Управление размерами** - изменение физических размеров принтов (мм) прямо из интерфейса
- ✅ **Отображение данных** - мокапы, размеры, артикулы, цвета
- ✅ **Поиск и фильтрация** - быстрый поиск принтов по артикулу
- 🔄 **Интеграция со скриптом** - возможность запуска скрипта раскладки

## 📋 Требования

- Adobe Photoshop 2023 (версия 24.0) или новее
- UXP Developer Tool

## 🚀 Установка

### Шаг 1: Скачайте библиотеку SheetJS

1. Перейдите на [cdn.sheetjs.com](https://cdn.sheetjs.com/)
2. Скачайте файл `xlsx.full.min.js` из последней версии
3. Поместите его в папку `lib/` проекта:
   ```
   photoshop-print-layout-plugin/
   ├── lib/
   │   └── xlsx.full.min.js  ← сюда
   ├── icons/
   ├── index.html
   ├── index.js
   └── ...
   ```

### Шаг 2: Установка через UXP Developer Tool

1. Откройте **UXP Developer Tool**
2. Нажмите **Add Plugin**
3. Выберите файл `manifest.json` из корневой папки плагина
4. Нажмите **Load**
5. Откройте Photoshop
6. Найдите плагин в меню **Plugins → Print Layout Manager**

## 📖 Использование

### 1. Подготовка таблицы XLSX

Ваша таблица должна содержать следующие колонки:

| A (Фото) | B (Размер) | C (ID заказа) | D (Наименование) | E (Цвет) | F (Артикул) |
|----------|------------|---------------|------------------|----------|-------------|
| [image]  | S (44-46)  | 4179761117    | Футболка...      | белый    | dva_mkd_0516|
| [image]  | M (46-48)  | 4179832645    | Футболка...      | черный   | spn_bka_7893|

**Важно:** 
- Первая строка - заголовки (игнорируется)
- Колонка F (Артикул) используется для сопоставления со слоями

### 2. Работа с плагином

#### Загрузка таблицы
1. Нажмите кнопку **📂 Загрузить XLSX таблицу**
2. Выберите ваш файл `.xlsx`
3. Плагин автоматически загрузит данные

#### Сопоставление со слоями
- Плагин автоматически ищет слои, в именах которых содержатся артикулы из таблицы
- Например, слой `dva_mkd_0516-XS-S` будет сопоставлен с записью артикула `dva_mkd_0516`

#### Выбор принта
- Кликните на принт в списке - соответствующий слой выделится в Photoshop
- Выберите слой в Photoshop - плагин покажет его данные

#### Изменение размера
1. Выберите принт из списка
2. Введите новые размеры (ширина × высота) в миллиметрах
3. Нажмите **Применить размер**
4. Слой автоматически масштабируется на холсте

#### Поиск
- Используйте поле поиска для фильтрации принтов по артикулу

### 3. Интеграция со скриптом раскладки

Для интеграции с вашим скриптом "Сборщик v.3.5":

1. Откройте файл `index.js`
2. Найдите функцию `runLayoutScript()`
3. Добавьте вызов вашего скрипта:

```javascript
async function runLayoutScript() {
    try {
        updateStatus('Запуск скрипта раскладки...');
        
        // Вызов вашего JSX скрипта
        const { executeAsModal } = require('photoshop').core;
        
        await executeAsModal(async () => {
            // Загрузить и выполнить ваш JSX скрипт
            const scriptPath = '/path/to/Sborshchik-v.3.5.jsx';
            // ... код выполнения скрипта
        });
        
        // После выполнения обновляем список
        refreshPrintsList();
        updateStatus('Скрипт выполнен');
    } catch (error) {
        console.error('Ошибка:', error);
    }
}
```

## 🎨 Структура проекта

```
photoshop-print-layout-plugin/
├── manifest.json          # Манифест плагина
├── package.json           # NPM конфигурация
├── index.html             # UI интерфейс
├── index.js               # Основная логика
├── styles.css             # Стили
├── lib/
│   └── xlsx.full.min.js   # Библиотека SheetJS (скачать отдельно)
├── icons/
│   ├── icon-light.png     # Иконка для светлой темы
│   └── icon-dark.png      # Иконка для тёмной темы
└── README.md
```

## 🔧 Технические детали

### Сопоставление слоёв

Плагин ищет слои по следующему принципу:
- Имя слоя должно **содержать** артикул из таблицы
- Например: `dva_mkd_0516-XS-S` ✅ сопоставится с `dva_mkd_0516`
- Регистр учитывается

### Работа с размерами

- Размеры слоёв конвертируются из пикселей в миллиметры (72 DPI)
- При изменении размера применяется пропорциональное масштабирование
- Формула: `мм = пиксели × 0.352778`

### API Photoshop

Используемые API:
- `app.activeDocument` - доступ к активному документу
- `document.layers` - работа со слоями
- `layer.bounds` - получение границ слоя
- `layer.scale()` - масштабирование слоя
- `document.activeLayers` - выделение слоёв

## ⚠️ Известные ограничения

1. **Мокапы из таблицы** - пока не реализованы (встроенные изображения в XLSX требуют дополнительной обработки)
2. **Повторяющиеся артикулы** - сопоставляется только первое совпадение
3. **Скрипт раскладки** - требует ручной интеграции

## 🐛 Отладка

Откройте Developer Console в Photoshop:
1. Перейдите в **Plugins → Development → Developer Console**
2. Выберите ваш плагин из списка
3. Смотрите логи и ошибки

## 📝 TODO

- [ ] Извлечение и отображение мокапов из XLSX
- [ ] Поддержка повторяющихся артикулов (множественное сопоставление)
- [ ] Автоматическая интеграция с JSX скриптами
- [ ] Экспорт данных обратно в XLSX с обновлёнными размерами
- [ ] Групповое изменение размеров
- [ ] История изменений с возможностью отката

## 📄 Лицензия

MIT License

## 👤 Автор

[@golovasya](https://github.com/golovasya)

---

**Примечание:** Для работы плагина необходимо скачать библиотеку SheetJS отдельно (см. Установка → Шаг 1).